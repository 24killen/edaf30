#ifndef TRACKS_H
#define TRACKS_H


class Tracks
{
    public:
        Tracks();
        virtual ~Tracks();
        Tracks(const Tracks& other);
        Tracks& operator=(const Tracks& other);
    protected:
    private:
};

#endif // TRACKS_H
